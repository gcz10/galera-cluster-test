---
- name: Restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted
    enabled: true

- name: Restart grafana
  ansible.builtin.systemd:
    name: grafana-server
    state: restarted
    enabled: true
